{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "module": "node16",
    "moduleResolution": "node16",
    "verbatimModuleSyntax": false,
    "lib": ["ES2022"],
    "types": ["node"],
    "outDir": "electron-dist",
    "rootDir": "electron",
    "noEmit": false,
    "sourceMap": true
  },
  "include": ["electron"],
  "//": "Test files are excluded here so build:electron never emits them into electron-dist and the production typecheck stays vitest-type-free; they are type-checked via tsconfig.test.json instead.",
  "exclude": ["electron/**/*.test.ts"]
}
